In the CI/CD pipeline on the staging environment, the test `test\_checkout\_process\_with\_discount` regularly fails. The test passes locally but fails on CI in \~40% of cases.

The development team claims that "the code is OK" and "it's probably a problem with the tests."

